import "./style.css";

const display_controller = (function (doc) {
  const initial_load = () => {
   
  };

  return {
    initial_load
  };
})(document);

document.onload = display_controller.initial_load();
